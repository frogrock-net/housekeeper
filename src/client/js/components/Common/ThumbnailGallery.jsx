// @flow

import * as React from 'react';
import styled from 'styled-components';

/**
 * The props accepted by the ThumbnailGallery component.
 *
 * - images - list of string - a list of string urls for the images to display.
 */
type Props = {
    images: ?(string[]),
};

/**
 * A ThumbnailGallery component.
 *
 * TODO: this.
 *
 * @param props the props, described above.
 */
const ThumbnailGallery = (props: Props) => {
    return <div />;
};

export default ThumbnailGallery;
